const axios = require("axios");
const { Driver } = require("../models/Driver");

const getDriverById = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    getDriverById
};