const axios = require("axios");
const { Driver } = require("../models/Driver");

const postDriver = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    postDriver
};