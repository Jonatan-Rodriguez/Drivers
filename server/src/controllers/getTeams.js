const axios = require("axios");
const { Driver } = require("../models/Driver");

const getTeams = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    getTeams
};