const axios = require("axios");
const { Driver } = require("../models/Driver");

const getDriversAll = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    getDriversAll
};